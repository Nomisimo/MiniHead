#pragma once

// ── WiFi Plugin (Standalone v1) ───────────────────────────────────
// Always-on webserver: cue sequencer, fader, rainbow.
// No discovery, no leader/follower, no UDP, no Art-Net.
// ─────────────────────────────────────────────────────────────────

#include "wifi_control.h"   // auto-registers itself as a plugin
